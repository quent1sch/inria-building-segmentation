# Install alongside requirements-api.txt for local development:
#   pip install -r requirements-api.txt -r requirements-dev.txt

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0    # async test support
pytest-cov>=5.0.0         # test coverage reporting
affine>=2.4.0             # required by mock_spatial_ref fixture in conftest.py

# Code quality (optional but recommended)
ruff>=0.4.0               # linter + formatter (replaces flake8 + black)